# 快速参考指南

## 连接线交互功能

### 🎨 颜色状态
| 状态 | 线条 + 箭头颜色 | 说明 |
|------|----------------|------|
| 🟠 悬停 | 橙色 | 鼠标悬停时 |
| 🔵 有规则 | 蓝色 | 已配置条件规则 |
| ⚫ 默认路径 | 深灰色 | 网关的默认分支 |
| ⚪ 普通 | 浅灰色 | 常规连接 |

### 🖱️ 交互操作

#### 悬停效果
```
鼠标移到连接线上
↓
线条 + 箭头变橙色
↓
宽度增加 (2px → 3px)
↓
显示删除按钮
```

#### 删除连接线
**方式一：点击删除按钮**
1. 悬停在连接线上
2. 等待红色删除按钮出现
3. 点击按钮

**方式二：使用键盘**
1. 点击选中连接线
2. 按 `Delete` 或 `Backspace`

#### 编辑条件
1. 点击连接线
2. 右侧面板打开
3. 配置可视化规则

### 📤 导出功能

#### JSON 导出
```
左侧工具栏 → "导出 JSON" → 下载 .json 文件
```

#### BPMN XML 导出
```
左侧工具栏 → "导出 BPMN XML" → 下载 .bpmn 文件
可在 Camunda Modeler 中打开
```

### 🎯 节点拖拽

从左侧工具栏拖拽节点到画布：
- ⚪ **开始节点** - 流程起点
- ⬛ **任务节点** - 执行任务
- ◆ **排他网关** - 单一路径选择
- ◇ **包容网关** - 多路径并行
- ⚫ **结束节点** - 流程终点

### 📋 快捷键

| 按键 | 功能 |
|------|------|
| `Delete` | 删除选中的节点或连接线 |
| `Backspace` | 删除选中的节点或连接线 |
| 鼠标拖拽 | 移动节点位置 |
| 点击空白处 | 取消选择 |

### 🔧 规则配置

#### 添加条件
1. 点击连接线
2. "添加条件" → "使用可视化规则"
3. 配置字段、操作符、值
4. 选择逻辑关系（AND/OR）

#### 操作符类型
- `=` 等于
- `≠` 不等于
- `>` 大于
- `<` 小于
- `≥` 大于等于
- `≤` 小于等于
- 包含、不包含
- 开头是、结尾是
- 为空、不为空
- 为真、为假

### 🌐 网关配置

#### 排他网关
- 只执行一条满足条件的路径
- 可设置默认路径
- 条件有优先级顺序

#### 包容网关
- 可同时执行多条满足条件的路径
- 支持复杂的业务场景
- 所有条件独立判断

### 💡 最佳实践

1. **命名规范** - 为节点和连接线设置清晰的名称
2. **条件完整** - 确保所有网关出口都有条件
3. **默认路径** - 为排他网关设置默认路径
4. **定期备份** - 使用 JSON 导出功能保存流程
5. **先设计后实现** - 规划好流程逻辑再开始绘制

### 🐛 常见问题

**Q: 删除按钮不显示？**  
A: 需要将鼠标悬停在连接线上，等待约 0.1 秒

**Q: 连接线颜色不对？**  
A: 检查是否设置了条件规则，蓝色表示有规则

**Q: 导出的 BPMN 无法打开？**  
A: 确保画布上至少有一个节点，使用 Camunda Modeler 8.0+ 版本

**Q: 如何撤销删除？**  
A: 目前不支持撤销，建议定期导出备份

### 📞 技术支持

查看详细文档：
- `README_EDGE_INTERACTIONS.md` - 连接线交互详解
- `README_BPMN_EXPORT.md` - BPMN 导出说明
- `README_RULES.md` - 规则系统指南
- `CHANGELOG_EDGE_ARROWS.md` - 箭头功能更新

---

**提示**：将鼠标悬停在任何元素上，通常会显示更多信息或操作选项！
